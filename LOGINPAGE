<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <main class="container">
    <form id="loginForm" class="card" novalidate aria-describedby="formHelp">
      <h1 class="title">Sign in</h1>

      <div id="formError" class="form-error" role="alert" aria-live="polite" hidden></div>

      <label for="email" class="label">Email</label>
      <input
        id="email"
        name="email"
        type="email"
        inputmode="email"
        autocomplete="email"
        class="input"
        required
        aria-describedby="emailHelp emailError"
        placeholder="you@example.com"
      />
      <div id="emailError" class="field-error" aria-live="polite"></div>

      <label for="password" class="label">Password</label>
      <div class="password-row">
        <input
          id="password"
          name="password"
          type="password"
          autocomplete="current-password"
          class="input"
          required
          minlength="8"
          aria-describedby="passwordHelp passwordError"
          placeholder="••••••••"
        />
        <button type="button" id="togglePassword" class="icon-btn" aria-label="Show password">Show</button>
      </div>
      <div id="passwordError" class="field-error" aria-live="polite"></div>

      <div class="row between center">
        <label class="checkbox">
          <input type="checkbox" id="remember" name="remember" />
          <span>Remember me</span>
        </label>
        <a href="#" class="link">Forgot password?</a>
      </div>

      <button id="submitBtn" class="btn primary" type="submit">Sign in</button>

      <p id="formHelp" class="muted">By signing in you agree to our <a href="#" class="link">terms</a>.</p>
    </form>
  </main>

  <script src="script.js" defer></script>
</body>
</html>
